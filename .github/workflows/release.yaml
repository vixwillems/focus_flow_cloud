name: Release

on:
  push:
    tags:
      - "v*"
      - "backend-v*"
      - "app-v*"

env:
  CARGO_TERM_COLOR: always

jobs:
  # ============================================================================
  # DOCS
  # ============================================================================
  docs:
    name: Build & Deploy Docs
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    defaults:
      run:
        working-directory: doc
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: doc/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build

      - name: Deploy to GitHub Pages
        if: startsWith(github.ref, 'refs/tags/')
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./doc/build

      - name: Cancel Workflow on Failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh run cancel ${{ github.run_id }}

  # ============================================================================
  # BACKEND
  # ============================================================================
  backend-build:
    if: startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/backend-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      actions: write
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Configure System
        run: |
          # Add arm64 architecture
          sudo dpkg --add-architecture arm64

          # Remove default sources and conflicting PGDG (Postgres) sources
          sudo rm -f /etc/apt/sources.list.d/ubuntu.sources
          sudo rm -f /etc/apt/sources.list.d/pgdg.sources.list

          # Rewrite sources.list to strictly separate architectures
          # amd64 -> archive.ubuntu.com / azure.archive.ubuntu.com
          # arm64 -> ports.ubuntu.com
          sudo tee /etc/apt/sources.list <<EOF
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ $(lsb_release -cs) main restricted universe multiverse
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ $(lsb_release -cs)-updates main restricted universe multiverse
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ $(lsb_release -cs)-backports main restricted universe multiverse
          deb [arch=amd64] http://azure.archive.ubuntu.com/ubuntu/ $(lsb_release -cs)-security main restricted universe multiverse
          
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ $(lsb_release -cs) main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ $(lsb_release -cs)-updates main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ $(lsb_release -cs)-backports main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ $(lsb_release -cs)-security main restricted universe multiverse
          EOF
          
          sudo apt-get update
          # Explicitly remove conflicting libpq packages to ensure clean state
          sudo apt-get remove -y libpq-dev libpq5
          sudo apt-get autoremove -y

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu,aarch64-unknown-linux-gnu

      - uses: swatinem/rust-cache@v2
        with:
          key: ${{ runner.os }}-rust-${{ hashFiles('**/Cargo.lock') }}
          workspaces: "."

      - name: Build binaries (amd64)
        working-directory: .
        run: |
          sudo apt-get install -y libpq-dev
          cargo build --release --target x86_64-unknown-linux-gnu --package focus_flow_cloud
          # Remove amd64 libs to clean up for arm64 build
          sudo apt-get remove -y libpq-dev libpq5

      - name: Build binaries (arm64)
        working-directory: .
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          PKG_CONFIG_libpq_aarch64_unknown_linux_gnu: /usr/lib/aarch64-linux-gnu/pkgconfig
          PKG_CONFIG_allow_cross_aarch64_unknown_linux_gnu: 1
          PKG_CONFIG_PATH: /usr/lib/aarch64-linux-gnu/pkgconfig
          PKG_CONFIG_ALLOW_CROSS: 1
        run: |
          sudo apt-get install -y gcc-aarch64-linux-gnu libpq-dev:arm64
          cargo build --release --target aarch64-unknown-linux-gnu --package focus_flow_cloud



      - name: Prepare binaries
        run: |
          cp ../target/x86_64-unknown-linux-gnu/release/focus_flow_cloud ./focus_flow_cloud-amd64
          cp ../target/aarch64-unknown-linux-gnu/release/focus_flow_cloud ./focus_flow_cloud-arm64

      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-bin
          path: |
            backend/focus_flow_cloud-amd64
            backend/focus_flow_cloud-arm64
          if-no-files-found: error

      - name: Extract version from Cargo.toml
        id: crate-version
        uses: colathro/crate-version@2.0.0
        with:
          file: "backend/Cargo.toml"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/focusflowcloud
          tags: |
            type=raw,value=${{ steps.crate-version.outputs.version }}
            type=raw,value=latest
            type=ref,event=tag

      - name: Build and push Docker
        uses: docker/build-push-action@v5
        with:
          context: backend
          file: backend/Dockerfile.production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          platforms: linux/amd64,linux/arm64
          cache-to: type=gha,mode=max

      - name: Cancel Workflow on Failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh run cancel ${{ github.run_id }}

  # ============================================================================
  # APP (Dioxus)
  # ============================================================================
  app-build:
    if: startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/app-v')
    name: Build App (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: macos
            platform: macos
          - os: ubuntu-latest
            target: linux
            platform: linux
          - os: windows-latest
            target: windows
            platform: windows
          - os: ubuntu-latest
            target: android
            platform: android
    defaults:
      run:
        working-directory: focus_flow_app
    permissions:
      contents: read
      actions: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: "focus_flow_app"

      - name: Install Linux dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libxdo-dev libayatana-appindicator3-dev

      - name: Setup Java (android)
        if: matrix.target == 'android'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Android NDK (android)
        if: matrix.target == 'android'
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27c

      - name: Add Android Rust targets
        if: matrix.target == 'android'
        run: rustup target add aarch64-linux-android armv7-linux-androideabi x86_64-linux-android

      - name: Install Dioxus CLI
        run: cargo install dioxus-cli --locked

      - name: Keep WRY JNI bridge from R8 stripping (android)
        if: matrix.target == 'android'
        run: |
          mkdir -p ~/.gradle/init.d
          cat > ~/.gradle/init.d/wry-proguard.gradle << 'EOF'
allprojects {
    afterEvaluate { project ->
        if (project.plugins.hasPlugin('com.android.application')) {
            def rulesFile = new File(project.buildDir, 'generated/wry-keep.pro')
            rulesFile.parentFile.mkdirs()
            rulesFile.text = '''\
-keep class dev.dioxus.main.** { *; }
-keepclassmembers class dev.dioxus.main.WryActivity {
    public void setWebView(...);
}
'''
            project.android.buildTypes.each { buildType ->
                if (buildType.name == 'release') {
                    buildType.proguardFile(rulesFile)
                }
            }
        }
    }
}
EOF

      - name: Bundle (${{ matrix.target }})
        if: matrix.target != 'android'
        run: dx bundle --release --platform ${{ matrix.platform }}

      - name: Bundle Android (aarch64)
        if: matrix.target == 'android'
        run: dx bundle --release --platform android --target aarch64-linux-android

      - name: Install create-dmg (macOS)
        if: matrix.target == 'macos'
        run: brew install create-dmg

      - name: Create DMG (macOS)
        if: matrix.target == 'macos'
        run: |
          APP_PATH=$(find target/dx/focus_flow_app/release/macos -name "*.app" -maxdepth 4 | head -1)
          create-dmg \
            --volname "Focus Flow" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --app-drop-link 600 185 \
            "focus_flow_app-macos.dmg" \
            "$APP_PATH"

      - name: Package Linux bundle
        if: matrix.target == 'linux'
        run: |
          BUNDLE_DIR=$(find target/dx/focus_flow_app/release/linux -maxdepth 2 -type d | head -1)
          tar -czf focus_flow_app-linux.tar.gz -C "$BUNDLE_DIR" .

      - name: Package Windows bundle
        if: matrix.target == 'windows'
        shell: pwsh
        run: |
          $bundleDir = Get-ChildItem -Path "target/dx/focus_flow_app/release/windows" -Directory | Select-Object -First 1
          Compress-Archive -Path "$($bundleDir.FullName)/*" -DestinationPath "focus_flow_app-windows.zip"

      - name: Sign APK (android)
        if: matrix.target == 'android'
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > release.jks
          APK_PATH=$(find target/dx/focus_flow_app/release/android -name "*.apk" | head -1)
          BUILD_TOOLS=$ANDROID_HOME/build-tools/$(ls $ANDROID_HOME/build-tools | sort | tail -1)
          $BUILD_TOOLS/apksigner sign \
            --ks release.jks \
            --ks-key-alias "${{ secrets.KEY_ALIAS }}" \
            --ks-pass pass:"${{ secrets.KEYSTORE_PASSWORD }}" \
            --key-pass pass:"${{ secrets.KEY_PASSWORD }}" \
            "$APK_PATH"

      - name: Package Android bundle
        if: matrix.target == 'android'
        run: |
          APK_PATH=$(find target/dx/focus_flow_app/release/android -name "*.apk" | head -1)
          cp "$APK_PATH" focus_flow_app-android.apk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-${{ matrix.target }}
          path: |
            focus_flow_app/focus_flow_app-macos.dmg
            focus_flow_app/focus_flow_app-linux.tar.gz
            focus_flow_app/focus_flow_app-windows.zip
            focus_flow_app/focus_flow_app-android.apk
          if-no-files-found: ignore

      - name: Cancel Workflow on Failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh run cancel ${{ github.run_id }}

  # ============================================================================
  # RELEASE
  # ============================================================================
  publish-release:
    name: Publish Release
    needs: [backend-build, app-build]
    if: startsWith(github.ref, 'refs/tags/v') || startsWith(github.ref, 'refs/tags/backend-v') || startsWith(github.ref, 'refs/tags/app-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release_assets
          merge-multiple: true

      - name: Display Downloaded Files
        run: ls -R release_assets

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            release_assets/**/focus_flow_cloud-amd64
            release_assets/**/focus_flow_cloud-arm64
            release_assets/**/*.dmg
            release_assets/**/*.tar.gz
            release_assets/**/*.zip
            release_assets/**/*.apk
          generate_release_notes: true
          fail_on_unmatched_files: false


